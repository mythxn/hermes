# Hermés - Deployment Instructions
Hermés is a FLASK web app that runs on your system **fully locally!**

##  Terminal Commands
    git clone https://github.com/mythxn/Hermes.git
    cd Hermes
    ./deploy.sh #if on linux/macOS
    ./deploy.bat #if on windows
